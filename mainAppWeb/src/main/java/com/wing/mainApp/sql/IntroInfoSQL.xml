<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="intromanager">
<!-- 	게시물 등록하기 -->
	<insert		id="insertIntroInfo"	parameterType="idata">
		INSERT
		INTO
			Introduce
		VALUES
			((SELECT NVL(MAX(intro_no), 0) + 1 FROM Introduce),
			#{mem_id}, #{intro_title}, #{intro_body},SYSDATE,'test','test1',#{gimg2},'10', SYSDATE,'N')
	</insert>
<!-- 	강사 소개글  질의	-->
	<select id="selectIntroList"		resultType="idata"
										parameterType="java.util.HashMap">
		SELECT
			intro_no,
			mem_id,
			intro_title,
			intro_body,
			intro_gimg AS gimg2  
		FROM
			Introduce		
	</select>
<!-- 	총 데이터 개수 구하기 질의 -->	
	<!-- 	지금 등록할 게시물의 일련번호를 알아내기 -->
	<select id="selectMax"	resultType="java.lang.Integer">
		SELECT
			NVL(MAX(intro_no), 0) + 1		AS NO
		FROM
		Introduce	
	</select>
<!-- 	총 데이터 개수 구하기 질의 -->	
	<select id="getTotal" resultType="java.lang.Integer">
		SELECT
			COUNT(*) AS TOTAL
		FROM
			Introduce
				
	</select>
	

</mapper>
